{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "start",
      "type": "shell",
      "command": "npx browser-sync start --server --files './*.html' --no-open --no-notify --directory",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
